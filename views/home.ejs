<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/home.css" />
    <title>File Link</title>
  </head>
  <body>
    <div id="container">
      <h1 id="title">File Link</h1>
      <p id="desc">
        Secure encrypted file sharing website built with
        <a href="https://expressjs.com/" target="_blank">Expressjs</a> .
      </p>

      <hr />

      <!-- file download url -->
      <% if(locals.fileLink != null) { %>
      <div id="file-link-message">
        <p>
          Your file <%= locals.fileName %> is uploaded. This link is valid for
          10 minutes.
        </p>
        <p>
          Download link for file is
          <a href="<%= locals.fileLink %>" target="_blank"
            ><%= locals.fileLink %></a
          >
        </p>
      </div>
      <% } %>

      <form action="/upload" method="POST" enctype="multipart/form-data">
        <label for="file">Upload file</label>
        <input type="file" name="file" id="file" required />
        <label for="password">
          Enter the password to encrypt your file. (optional)
        </label>
        <input
          type="password"
          name="password"
          id="password"
          placeholder="Enter the password"
        />
        <button type="submit">Share</button>
      </form>
    </div>
  </body>
</html>
