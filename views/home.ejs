<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File Link</title>
  </head>
  <body>
    <h1>File Link</h1>
    <p>
      Secure encrypted file sharing website built in
      <a href="https://expressjs.com/" target="_blank">Expressjs</a> .
    </p>

    <!-- file download url -->
    <% if(locals.fileLink != null) { %> Your file <%= locals.fileName %> is
    uploaded. Download link for file is
    <a href="<%= locals.fileLink %>"><%= locals.fileLink %></a>
    <% } %>

    <form action="/upload" method="POST" enctype="multipart/form-data">
      <label for="file">Upload file</label>
      <input type="file" name="file" id="file" />
      <label for="password">
        Enter the password to encrypt your file. (optional)
      </label>
      <input
        type="password"
        name="password"
        id="password"
        placeholder="Enter the password"
      />
      <button type="submit">Share</button>
    </form>
  </body>
</html>
